
```java
catch (BadCredentialsException e) {
    // 再次查询数据库检查用户是否存在
    User user = userMapper.findByUsername(request.getUsername());
    
    if (user == null) {
        log.warn("【登录失败】用户名: {}，原因: 账号不存在...", ...);
    } else {
        log.warn("【登录失败】用户名: {}，原因: 密码错误...", ...);
    }
    throw new BusinessException(ResultCode.INVALID_CREDENTIALS);
}
```

---

## 📊 修复效果

### 账号不存在
```
INFO  【登录尝试】用户名: user1
WARN  【登录失败】用户名: user1，原因: 账号不存在 ✅
```

### 密码错误
```
INFO  【登录尝试】用户名: user
WARN  【登录失败】用户名: user，原因: 密码错误 ✅
```

### 登录成功
```
INFO  【登录尝试】用户名: user
INFO  【认证成功】用户名: user
INFO  【登录成功】用户名: user，JWT Token 已生成 ✅
```

---

## 🚀 应用修复

在 IDEA 中执行：

```
Ctrl + F9   编译
Ctrl + F2   停止
Shift + F10 启动
```

---

## 📝 代码修改

**文件**：`AuthServiceImpl.java`  
**方法**：`login()` 的 `catch (BadCredentialsException e)` 块  
**行号**：第 163-176 行

**关键改动**：
```java
// 改前：依赖异常信息（不可靠）
if (e.getMessage().contains("password does not match")) { ... }

// 改后：直接查询数据库（可靠）
User user = userMapper.findByUsername(request.getUsername());
if (user == null) { ... }
```

---

完成！现在编译并重启应用，测试应该正确显示错误原因了。✨
